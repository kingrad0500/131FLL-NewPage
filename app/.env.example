# Preview builds should omit these values (or leave indexing false).
PUBLIC_ALLOW_INDEXING=false

# Set both values only in the final Vercel Production environment after launch
# approval. Use the canonical HTTPS origin with no path.
PUBLIC_SITE_URL=https://www.example.com
