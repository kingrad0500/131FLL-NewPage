{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    // The content audit runs under Node's native TypeScript type stripping.
    "types": ["node"]
  }
}
